#include<stdio.h>
int main()
{ 
      float areaOfCircle,radius;
      printf("Enter the Radius : ");
      scanf("%f",&radius);
      areaOfCircle=3.14*r*r;
      printf("Area is %f",radius);
      return 0;
}
